import { Calculator, BookOpen, FlaskConical, History, GraduationCap, ChevronLeft, CheckCircle2, XCircle } from 'lucide-react';

export const Icons = {
  Calculator,
  BookOpen,
  FlaskConical,
  History,
  GraduationCap,
  ChevronLeft,
  CheckCircle2,
  XCircle
};

export type IconName = keyof typeof Icons;
